# Task: Fix Business Logic Errors and Export

- `[ ]` Update `pubspec.yaml` with missing dependencies (`dio`, `get_it`, `flutter_bloc`, `bloc`, `toastification`, `ant_design_flutter`)
- `[ ]` Fix imports in `lib/src/business_logic/` (Replace `package:khatoon_container/` with `package:khatoon_shared/src/business_logic/`)
- `[ ]` Resolve missing class and type errors in `injection_container.dart`
- `[ ]` Fix logic errors in BLoCs and UseCases (Payment, Product, Animal, AI Voice)
- `[ ]` Update `lib/index.dart` to export valid business logic components and remove broken exports
- `[ ]` Verify changes with `flutter analyze`
