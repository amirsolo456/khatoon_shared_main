enum Gender { male, female, other }

enum HealthStatus { good, fair, poor, sick, dead }

enum ReproductionStatus { ready, notReady, pregnant, lactating }

enum SellerType { individual, company, agent }

enum VerificationStatus { pending, verified, rejected }

enum MembershipLevel { bronze, silver, gold, platinum }

enum EventType { birth, death, sale, purchase, medical, vaccination, feed, weight, milking, reproduction }

enum ReproductionType { mating, insemination, pregnancyCheck, birth, abortion }

enum TransactionType { purchase, sale, transfer, death, slaughter }

enum BaseMasterDetailPagesOpenType { list, detail, add, edit }

enum PageActionsTypes { create, update, delete, view }
